name: void_space
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter

  # Networking / metadata
  http: ^1.2.0
  html: ^0.15.4

  # Local storage
  sqflite: ^2.3.2
  path: ^1.9.0 # Restored to a flexible, stable version
  shared_preferences: ^2.2.2 

  # UI
  flutter_staggered_grid_view: ^0.7.0
  google_fonts: ^6.2.1

  # System
  url_launcher: ^6.2.6
  local_auth: ^2.1.6 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true